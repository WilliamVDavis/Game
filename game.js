var config = {
    type: Phaser.AUTO,
    width: 800,
    height: 600,
    physics: {
        default:'arcade',
        arcade: {
        }
    }
,  scene: ( ex1 )
};
var Phaser;
var AUTO;
 var game = new Phaser.Game(config);